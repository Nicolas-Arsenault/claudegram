# Claude Telegram Bridge Configuration
# Copy this file to .env and fill in the values

# Required: Your Telegram Bot Token
# Create a bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# Required: Comma-separated list of Telegram user IDs allowed to use the bot
# Get your user ID by messaging @userinfobot on Telegram
ALLOWED_USER_IDS=

# Optional: AI backend to use - 'claude' or 'codex' (default: claude)
# - claude: Uses Claude Code CLI (requires 'claude' command)
# - codex: Uses OpenAI Codex CLI (requires 'codex' command, run 'npm i -g @openai/codex')
# AI_BACKEND=claude

# Optional: Directory for screenshot output (default: ./screenshots)
# SCREENSHOT_OUTPUT_DIR=./screenshots

# Optional: Directory for received images (default: ./inputs)
# INPUT_IMAGE_DIR=./inputs

# Optional: Session idle timeout in milliseconds (default: 1800000 = 30 minutes)
# SESSION_IDLE_TIMEOUT_MS=1800000
